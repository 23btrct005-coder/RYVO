{
  "name": "ryvo-monorepo",
  "private": true,
  "scripts": {
    "build": "if [ -d ryvo-rider ]; then cd ryvo-rider && npm run build; else npm run build; fi"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.115.0",
    "pg": "^8.22.0"
  }
}
